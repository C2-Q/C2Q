# Compatibility lock-file entry point.
#
# Earlier artifact drafts used this file as a monolithic lock file. The current
# reviewer path uses the split, pinned requirement files instead:
#   requirements-core.txt
#   requirements-parser.txt
#   requirements-recommender.txt
#   requirements-artifact.txt
#   requirements-dev.txt
#
# Keep this file as a safe redirect so old commands do not install the stale
# Qiskit 1.x stack from previous drafts.
-r requirements-dev.txt
