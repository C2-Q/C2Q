# Compatibility entry point for users who run:
#   python -m pip install -r requirements.txt
#
# The reviewed artifact dependency profile is split into smaller pinned files.
# `requirements-dev.txt` includes the core, parser, recommender, and pytest
# dependencies used by the source-checkout reviewer path.
-r requirements-dev.txt
