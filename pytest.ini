[pytest]
testpaths = src/tests
addopts = -m "unit and not model and not paper" -p no:warnings
markers =
    unit: Fast tests that do not require external model assets.
    model: Tests that require parser model files.
    paper: Long-running paper-scale tests (time-consuming).
